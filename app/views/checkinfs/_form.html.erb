<%= form_for(@checkinf, :html=>{:enctype=>'multipart/form-data'}) do |f| %>
  <% if @checkinf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@checkinf.errors.count, "error") %> prohibited this checkinf from being saved:</h2>

      <ul>
      <% @checkinf.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
  	<%= f.label :projectname %>
  	<%= collection_select :project, :projname, Project.all, :id, :projname%>
  </div>
  <div class="field">
  	<%= f.label :checkclass %>
  	<%= collection_select :checkclass, :checkclassname, CheckClass.all, :id, :checkclassname %>
  </div>
  <div class="field">
  	<%= :username%>
  	<%= collection_select :user, :username, User.all, :id, :username%>
  </div>
  
  <!--提交文件域，重构代码以此进行-->
  <div class="field">
  	<%= f.label :back_information%>
  	<%= file_field_tag(:img_file, :size=>'33')%>
  </div>
  <div class="field">
    <%= f.label :checktime %><br />
    <%= f.time_select :checktime %>
  </div>
  <div class="field">
    <%= f.label :remarkinf %><br />
    <%= f.text_area :remarkinf %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
