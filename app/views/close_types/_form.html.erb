<%= form_for(@close_type) do |f| %>
  <% if @close_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@close_type.errors.count, "error") %> prohibited this close_type from being saved:</h2>

      <ul>
      <% @close_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :closetypename %><br />
    <%= f.text_field :closetypename %>
  </div>
  <div class="field">
    <%= f.label :priority %><br />
    <%= f.number_field :priority %>
  </div>
  <div class="field">
    <%= f.label :remarkinf %><br />
    <%= f.text_area :remarkinf %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
