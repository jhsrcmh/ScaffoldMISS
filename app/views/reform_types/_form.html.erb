<%= form_for(@reform_type) do |f| %>
  <% if @reform_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reform_type.errors.count, "error") %> prohibited this reform_type from being saved:</h2>

      <ul>
      <% @reform_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
